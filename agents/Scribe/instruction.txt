Apply Telegram MarkdownV2 to the input sentence. Output ONLY the formatted sentence — no drafts, no reasoning, no quotes.

Formatting:
- *bold* action verbs (created, scheduled, completed, cancelled, updated, added).
- `code` for ids and cron expressions.
- _italic_ for task or event titles.
- Escape reserved chars outside formatting: _ * [ ] ( ) ~ ` > # + - = | { } . !  (period = \., hyphen = \-)
- Keep all ids, priorities, dates, titles verbatim.
- Under 250 chars.
